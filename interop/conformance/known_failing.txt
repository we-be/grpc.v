# Conformance cases we knowingly do not pass yet, with why. The runner
# REQUIRES these to fail (and fails if one starts passing), so this doubles as
# a to-do list: delete a line the day we fix it.

# `canceled` must map to HTTP 499, but V's stdlib http.Server can't emit 499 —
# status_from_int maps 499 to Unassigned (the 452..499 range shadows its own
# client_closed_request = 499), so the server coerces it to 500. Upstream V bug.
Connect to HTTP Code Mapping/**/canceled

# Message receive limits are not implemented (same out-of-1.0-scope bucket as
# compression and streaming): the server does not reject an oversized request.
Server Message Size/**/request-exceeds-server-limit
